% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lic.R
\name{nm_lic_info}
\alias{nm_lic_info}
\title{Parse NONMEM License Information}
\usage{
nm_lic_info(nm_starter)
}
\arguments{
\item{nm_starter}{The script of the NONMEM executable. See details above.}
}
\value{
A list with components:
\itemize{
\item \code{registered_to} (\code{character(1)}): Name/email the license is registered to, or \code{NA}.
\item \code{expiration_date} (\code{character(1)}): License expiration date as printed by NONMEM, or \code{NA}.
\item \code{current_date} (\code{character(1)}): Current date as printed by NONMEM, or \code{NA}.
\item \code{expired} (\code{logical(1)}): \code{TRUE} if NONMEM reports the license expired, otherwise \code{FALSE}.
\item \code{days_left} (\code{integer(1)}): Days until expiration if reported by NONMEM, otherwise \code{NA_integer_}.
\item \code{error} (\code{character(1)}): Error message if the check failed; otherwise \code{NA}.
}
}
\description{
Runs a short NONMEM test via \code{\link[=test_nm]{test_nm()}} and parses license-related
information from the NONMEM console output. Returns the registered user,
expiration/current dates, whether the license is expired, and the days
remaining (if available).
}
\details{
The \code{nm_starter} is either a \code{bat} file on Windows (e.g. \code{nm74/run/nmfe74.bat})
or a shell script on Linux (e.g. \code{nm751/run/nmfe75}). The NONMEM run is
executed only to capture console output that carries the license banner.
}
\examples{
\dontrun{
nm_lic_info("/opt/nonmem/nm751/run/nmfe75")
}

}
